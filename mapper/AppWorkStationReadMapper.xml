<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="readAppWorkStationMapper">			
	
	<select id="readAppWorkStation">
		select * from app_work_station where work_station_id = #{workStationId};
	</select>

	<select id="appEmployee_BBRG7">         
		select * from app_employee where work_station_id = #{workStationId};
	</select>
  
</mapper>
