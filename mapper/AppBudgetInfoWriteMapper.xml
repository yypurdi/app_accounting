<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="writeAppBudgetInfoMapper">			
	
	<update id="writeAppBudgetInfo">
		update app_budget_info set budget_info_id = #{budgetInfoId},
		year = #{year},
		month = #{month},
		budget = #{budget},
		realization = #{realization} where budget_info_id = #{budgetInfoId};
	</update>

</mapper>
