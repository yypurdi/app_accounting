<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="writeAppAdjustmentJournalMapper">			
	
	<update id="writeAppAdjustmentJournal">
		update app_adjustment_journal set adjustment_journal_id = #{adjustmentJournalId},
		account_no = #{accountNo},
		date = #{date},
		reference_no = #{referenceNo},
		in_or_debit = #{inOrDebit},
		out_or_credit = #{outOrCredit} where adjustment_journal_id = #{adjustmentJournalId};
	</update>

</mapper>
