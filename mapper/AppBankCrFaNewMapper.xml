<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="newAppBankCrFaMapper">			
	
	<insert id="newAppBankCrFa">
		insert into app_bank_cr_fa(bank_cash_receive_detail_id,bank_cash_receive_id,account_no,department_id,amount,job_order_no) 
		values (#{bankCashReceiveDetailId},#{bankCashReceiveId},#{accountNo},#{departmentId},#{amount},#{jobOrderNo});
	</insert>

</mapper>
