<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="readAppRecurringJournalMapper">			
	
	<select id="readAppRecurringJournal">
		select * from app_recurring_journal where recurring_journal_id = #{recurringJournalId};
	</select>

</mapper>
