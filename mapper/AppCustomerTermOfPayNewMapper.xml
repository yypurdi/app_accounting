<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="newAppCustomerTermOfPayMapper">			
	
	<insert id="newAppCustomerTermOfPay">
		insert into app_customer_term_of_pay(customer_id,term_of_payment_id) 
		values (#{customerId},#{termOfPaymentId});
	</insert>

</mapper>
