/*
 * script route generated by KOMA
 * @author : Yan Yan Purdiansah
 */    
var express = require('express');
var router = express.Router();
var dateFormat = require('dateformat');    
var database = require('./dao');
var dao = new database.Dao();    
var sessionChecker = require('./security');
var appresource = {
    Title:'AppProjectRead',
    Heading:'AppProject',
    ProjectId:{label :'ProjectId'},
    Name:{label :'Name'},
    ProjectManager:{label :'ProjectManager'},
    Details:{label :'Details'},
    Status:{label :'Status'},
    CustomerId:{label :'CustomerId'},
    IsJobOrder:{label :'IsJobOrder'},
    IsActive:{label :'IsActive'},
    JobOrderNo:{label :'JobOrderNo'},
    IsUsePhase:{label :'IsUsePhase'},
    OrderDate:{label :'OrderDate'},
    DeliveryDate:{label :'DeliveryDate'},
    ContactPerson:{label :'ContactPerson'},
    PercentageFinish:{label :'PercentageFinish'},
    EstimationCostBudget:{label :'EstimationCostBudget'},
    EstimationIncomeBudget:{label :'EstimationIncomeBudget'},
    }

router.get('/', sessionChecker, function(req, res, next) {
    var jsonObj = req.query;
    jsonObj.rowid = 0;
    req.session.previouspath = 'readAppProject?'+req._parsedUrl.query;
    parameter = {
        session: req.session,
        cookies: req.cookies,
        resource: appresource,
        dateFormat: dateFormat,
    }
    var readAppProject = new Promise((resolve, reject) => {
        setTimeout(() => {
            dao.readAppProject(jsonObj,function(err,rows) {
                if(err) res.status(err.code).end();
                else{
                    if(rows[0]===undefined) res.redirect('/searchAppProject');
                    else{ 
                        jsonObj = rows[0];
                        resolve(jsonObj);
                    }
                }
            });
        },1000);
    });
    
    var sendResult = (results) => {
        console.log(jsonObj)
        parameter.record = jsonObj;        
        res.render('AppProjectReadPage',parameter);
    }    
    Promise.all([readAppProject]).then(sendResult);
});

module.exports = router;
