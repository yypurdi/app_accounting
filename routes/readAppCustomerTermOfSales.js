/*
 * script route generated by KOMA
 * @author : Yan Yan Purdiansah
 */    
var express = require('express');
var router = express.Router();
var dateFormat = require('dateformat');    
var database = require('./dao');
var dao = new database.Dao();    
var sessionChecker = require('./security');
var appresource = {
    Title:'AppCustomerTermOfSalesRead',
    Heading:'AppCustomerTermOfSales',
    CustomerId:{label :'CustomerId'},
    TermOfSalesId:{label :'TermOfSalesId'},
    }

router.get('/', sessionChecker, function(req, res, next) {
    var jsonObj = req.query;
    jsonObj.rowid = 0;
    req.session.previouspath = 'readAppCustomerTermOfSales?'+req._parsedUrl.query;
    parameter = {
        session: req.session,
        cookies: req.cookies,
        resource: appresource,
        dateFormat: dateFormat,
    }
    var readAppCustomerTermOfSales = new Promise((resolve, reject) => {
        setTimeout(() => {
            dao.readAppCustomerTermOfSales(jsonObj,function(err,rows) {
                if(err) res.status(err.code).end();
                else{
                    if(rows[0]===undefined) res.redirect('/searchAppCustomerTermOfSales');
                    else{ 
                        jsonObj = rows[0];
                        resolve(jsonObj);
                    }
                }
            });
        },1000);
    });
    
    var sendResult = (results) => {
        console.log(jsonObj)
        parameter.record = jsonObj;        
        res.render('AppCustomerTermOfSalesReadPage',parameter);
    }    
    Promise.all([readAppCustomerTermOfSales]).then(sendResult);
});

module.exports = router;
