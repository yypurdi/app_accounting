/*
 * script route generated by KOMA
 * @author : Yan Yan Purdiansah
 */    
var express = require('express');
var router = express.Router();
var dateFormat = require('dateformat');    
var database = require('./dao');
var dao = new database.Dao();    
var sessionChecker = require('./security');
var appresource = {
    Title:'AppGeneralLedgerDetailRead',
    Heading:'AppGeneralLedgerDetail',
    GeneralLedgerDetailId:{label :'GeneralLedgerDetailId'},
    AccountNo:{label :'AccountNo'},
    Date:{label :'Date'},
    Note:{label :'Note'},
    Debit:{label :'Debit'},
    Credit:{label :'Credit'},
    Balance:{label :'Balance'},
    ReferenceNo:{label :'ReferenceNo'},
    }

router.get('/', sessionChecker, function(req, res, next) {
    var jsonObj = req.query;
    jsonObj.rowid = 0;
    req.session.previouspath = 'readAppGeneralLedgerDetail?'+req._parsedUrl.query;
    parameter = {
        session: req.session,
        cookies: req.cookies,
        resource: appresource,
        dateFormat: dateFormat,
    }
    var readAppGeneralLedgerDetail = new Promise((resolve, reject) => {
        setTimeout(() => {
            dao.readAppGeneralLedgerDetail(jsonObj,function(err,rows) {
                if(err) res.status(err.code).end();
                else{
                    if(rows[0]===undefined) res.redirect('/searchAppGeneralLedgerDetail');
                    else{ 
                        jsonObj = rows[0];
                        resolve(jsonObj);
                    }
                }
            });
        },1000);
    });
    
    var sendResult = (results) => {
        console.log(jsonObj)
        parameter.record = jsonObj;        
        res.render('AppGeneralLedgerDetailReadPage',parameter);
    }    
    Promise.all([readAppGeneralLedgerDetail]).then(sendResult);
});

module.exports = router;
