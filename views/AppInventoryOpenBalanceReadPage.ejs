<!DOCTYPE html>
<html>
<head lang="en">
<% include head %>
</head>
<body>
<% include side-nav %>
<div id="main">
<% include header-nav %>
<div class="container-fluid">
<section id="main-content">

    <ul class="nav nav-tabs" id="myTab" role="tablist">
      <li class="nav-item">
        <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true"><h4><%=resource.Heading%></h4></a>
      </li>
    </ul>
    
    <div class="tab-content" id="myTabContent">

        <div class="tab-pane fade in active" id="home" role="tabpanel" aria-labelledby="home-tab">	
            <div style="width:40%">
            <form class="form-horizontal">
                <table class="table table-striped">
                    <tr class="heading-tr">
                        <th> <%=resource.Heading%></th>
                        <th></th>
                    </tr>
                    <tr>
                        <td><%=resource.InventoryOpeningBalanceId.label%></td>
                        <td><%=record.inventory_opening_balance_id%></td>						
                    </tr><tr>
                        <td><%=resource.LocationMaster.label%></td>
                        <td><%=record.location_master%></td>						
                    </tr><tr>
                        <td><%=resource.ProductId.label%></td>
                        <td><%=record.product_id%></td>						
                    </tr><tr>
                        <td><%=resource.BeginningBalance.label%></td>
                        <td><%=record.beginning_balance%></td>						
                    </tr><tr>
                        <td><%=resource.UnitCost.label%></td>
                        <td><%=record.unit_cost%></td>						
                    </tr><tr>
                        <td><%=resource.IsSerialNumber.label%></td>
                        <td><%=record.is_serial_number%></td>						
                    </tr><tr>
                        <td><%=resource.IsLotNumber.label%></td>
                        <td><%=record.is_lot_number%></td>						
                    </tr>
                
                </table>
                <% if(session.user_acl=='ROLE_ADMIN') { %>
                <button type="submit" class="btn btn-default"><a href="/writeAppInventoryOpenBalance?inventoryOpeningBalanceId=<%=record.inventory_opening_balance_id%>">Edit</a></button>
                <button type="submit" class="btn btn-default"><a href="/deleteAppInventoryOpenBalance?inventoryOpeningBalanceId=<%=record.inventory_opening_balance_id%>">Delete</a></button>
                <% } %>
                <button type="button" class="btn btn-default" onclick="javascript:window.history.back()">Back</button>
            </form>
            </div>
        </div>
        
    </div>

</section>
</div>
</div>
<% include footer %>
</body>
</html>
