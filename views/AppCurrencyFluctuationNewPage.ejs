<!DOCTYPE html>
<html>
<head lang="en">
<% include head %>
</head>
<body>
<% include side-nav %>
<div id="main">
<% include header-nav %>
<div class="container-fluid">
<section id="main-content">

    <div style="width:40%">
    <h3><%=resource.Title%></h3>
    <form class="form-horizontal" action="/newAppCurrencyFluctuation" method="post">
        <table class="table table-striped">
            <tr class="heading-tr">
                <th><%=resource.Heading%></th>
                <th style="text-align:right"><%=statusMessage%></th>
            </tr>
            <tr>
                <td><%=resource.CurrencyFluctuationId.label%></td>
                <td><input type="text" class="form-control" name="currencyFluctuationId" value="<%=record.currencyFluctuationId%>"/></td>
            </tr><tr>
                <td><%=resource.CurrencyId.label%></td>
                <td><input type="text" class="form-control" name="currencyId" value="<%=record.currencyId%>"/></td>
            </tr><tr>
                <td><%=resource.ByDate.label%></td>
                <td><input type="text" class="form-control" name="byDate" value="<%=record.byDate%>"/></td>
            </tr><tr>
                <td><%=resource.ExchangeRate.label%></td>
                <td><input type="text" class="form-control" name="exchangeRate" value="<%=record.exchangeRate%>"/></td>
            </tr>
        
        </table>
        <button type="submit" class="btn btn-default">Add</button>
        <% if(statusCode == 200) { %>
        <button type="button" class="btn btn-default"><a href="/<%=session.previouspath%>">Back</a></button>
        <% }else{ %>
        <button type="button" class="btn btn-default" onclick="javascript:window.history.back()">Cancel</button>
        <% } %>
    </form>
    </div>

</section>
</div>
</div>
<% include footer %>
</body>
</html>
