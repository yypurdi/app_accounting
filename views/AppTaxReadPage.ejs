<!DOCTYPE html>
<html>
<head lang="en">
<% include head %>
</head>
<body>
<% include side-nav %>
<div id="main">
<% include header-nav %>
<div class="container-fluid">
<section id="main-content">

    <ul class="nav nav-tabs" id="myTab" role="tablist">
      <li class="nav-item">
        <a class="nav-link active" id="home-tab" data-toggle="tab" href="#home" role="tab" aria-controls="home" aria-selected="true"><h4><%=resource.Heading%></h4></a>
      </li>
    </ul>
    
    <div class="tab-content" id="myTabContent">

        <div class="tab-pane fade in active" id="home" role="tabpanel" aria-labelledby="home-tab">	
            <div style="width:40%">
            <form class="form-horizontal">
                <table class="table table-striped">
                    <tr class="heading-tr">
                        <th> <%=resource.Heading%></th>
                        <th></th>
                    </tr>
                    <tr>
                        <td><%=resource.TaxId.label%></td>
                        <td><%=record.tax_id%></td>						
                    </tr><tr>
                        <td><%=resource.TaxCode.label%></td>
                        <td><%=record.tax_code%></td>						
                    </tr><tr>
                        <td><%=resource.Tax.label%></td>
                        <td><%=record.tax%></td>						
                    </tr><tr>
                        <td><%=resource.Percentage.label%></td>
                        <td><%=record.percentage%></td>						
                    </tr><tr>
                        <td><%=resource.IsCogsDeductible.label%></td>
                        <td><%=record.is_cogs_deductible%></td>						
                    </tr><tr>
                        <td><%=resource.AcctCodePurchaseTax.label%></td>
                        <td><%=record.acct_code_purchase_tax%></td>						
                    </tr><tr>
                        <td><%=resource.AcctCodeSales.label%></td>
                        <td><%=record.acct_code_sales%></td>						
                    </tr><tr>
                        <td><%=resource.Details.label%></td>
                        <td><%=record.details%></td>						
                    </tr>
                
                </table>
                <% if(session.user_acl=='ROLE_ADMIN') { %>
                <button type="submit" class="btn btn-default"><a href="/writeAppTax?taxId=<%=record.tax_id%>">Edit</a></button>
                <button type="submit" class="btn btn-default"><a href="/deleteAppTax?taxId=<%=record.tax_id%>">Delete</a></button>
                <% } %>
                <button type="button" class="btn btn-default" onclick="javascript:window.history.back()">Back</button>
            </form>
            </div>
        </div>
        
    </div>

</section>
</div>
</div>
<% include footer %>
</body>
</html>
